# 🥁 太鼓自制谱面投稿平台

基于 Flask 的太鼓自制谱面投稿、审核与社区分享系统。用户注册后可上传 TJA + OGG 文件投稿，管理员审核通过后自动上传至 [taiko.asia](https://taiko.asia) 服务器，并在创作者社区展示。

## ✨ 功能

- **用户系统** — 注册 / 登录 / 个人投稿面板
- **谱面投稿** — 上传 TJA + OGG 文件，选择歌曲分类
- **审核系统** — 管理员审核投稿，通过后自动上传到 taiko.asia
- **投稿管理** — 查看上传时间、审核进度、取消审核中投稿
- **创作者社区** — 通过的谱面自动发布，支持点赞与评论
- **敏感词过滤** — 评论内容自动过滤敏感词

## 🚀 本地开发

```bash
# 创建虚拟环境
python -m venv .venv
# Windows
.venv\Scripts\activate
# Linux/Mac
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动开发服务器
python app.py
```

启动后访问 `http://127.0.0.1:5000`

**默认管理员账户：** `admin` / `admin123`（可通过环境变量 `ADMIN_USERNAME` / `ADMIN_PASSWORD` 修改）

## 🌐 Ubuntu 部署

```bash
sudo bash setup.sh
```

部署脚本会引导你设置：
- 管理员用户名和密码
- 服务端口（默认 5000）
- 域名（可选，用于 Nginx 配置）

部署完成后自动配置 Gunicorn + Nginx + systemd 服务。

## 📁 项目结构

```
├── app.py              # Flask 主应用
├── config.py           # 配置
├── models.py           # 数据库模型
├── forms.py            # 表单定义
├── utils.py            # 工具函数（上传、敏感词过滤）
├── setup.sh            # Ubuntu 部署脚本
├── requirements.txt    # Python 依赖
├── static/
│   └── style.css       # 深色主题样式
└── templates/          # Jinja2 模板
    ├── base.html
    ├── register.html
    ├── login.html
    ├── upload.html
    ├── dashboard.html
    ├── community.html
    ├── submission_detail.html
    └── admin.html
```

## 🛠 技术栈

- **后端** — Flask, Flask-SQLAlchemy, Flask-Login, Flask-WTF
- **数据库** — SQLite
- **前端** — Jinja2 + Vanilla CSS (深色主题)
- **生产部署** — Gunicorn + Nginx + systemd
